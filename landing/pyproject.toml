[project]
name = "landing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "docker>=7.1.0",
    "flask>=3.1.3",
    "gunicorn>=23.0.0",
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "ruff>=0.16.5",
]
